import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: ['static.lenskart.com','static1.lenskart.com','static5.lenskart.com','res.cloudinary.com'],
  },
};

export default nextConfig;
